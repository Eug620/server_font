{
  "name": "server-font",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "font:delete": "rm -rf VITE_FONT/*",
    "font:clone": "rm -rf VITE_FONT/* && cd VITE_FONT && git clone https://github.com/Eug620/vite.git && cd vite && yarn && yarn build",
    "font:copy": "rm -rf public/* && cp -r ./VITE_FONT/vite/dist/* ./public/",
    "d:c": "npm run font:clone && npm run font:copy"
  },
  "dependencies": {
    "cookie-parser": "~1.4.4",
    "debug": "~2.6.9",
    "express": "~4.16.1",
    "http-errors": "~1.6.3",
    "jade": "~1.11.0",
    "morgan": "~1.9.1"
  }
}
